kind: Command
name: update-lib
description:
  short: update lib artifacts
gardenCommand:
  - run
  - task
  - copy-lib-artifacts
---
kind: Command
name: test-lib
description:
  short: run test script from artifacts
gardenCommand:
  - exec
  - app
  - /bin/sh /usr/share/nginx/html/lib/test
